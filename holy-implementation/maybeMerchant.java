// intuitively, the merchant needs to be an entirely independent component of the code base -- it is foreseeable that the merchant might have its own set of methods -- but for the time being, i dont know how to implement the merchant being an ArrayList to have size() and get(int) accessible 
import java.util.*;

public class maybeMerchant {
    public List<Card> Merchant = new ArrayList<>();
    
}
